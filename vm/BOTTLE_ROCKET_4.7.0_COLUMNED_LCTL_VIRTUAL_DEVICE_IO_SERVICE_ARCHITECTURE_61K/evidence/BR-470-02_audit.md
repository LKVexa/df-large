# BR-470-02 — Console device

Status: **OPERATIONAL (host-reference)**

Console read/write are bounded and capability-gated; POSIX output flushes every write including zero-length flush.

Production source boundary: 60945 bytes (<61,000). Device ABI specification: `spec/DEVICE_ABI.json`.
