# BR-470-05 — Entropy device

Status: **OPERATIONAL (host-reference)**

Production entropy is explicitly nondeterministic; deterministic seeded replay and fail-closed error paths are tested.

Production source boundary: 60945 bytes (<61,000). Device ABI specification: `spec/DEVICE_ABI.json`.
