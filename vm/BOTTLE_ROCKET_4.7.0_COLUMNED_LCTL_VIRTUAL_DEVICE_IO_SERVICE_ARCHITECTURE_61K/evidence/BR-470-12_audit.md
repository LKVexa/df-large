# BR-470-12 — Device quotas

Status: **OPERATIONAL (host-reference)**

Service calls, host calls, transfer bytes, storage writes, entropy requests and mailbox depth are bounded and fail closed.

Production source boundary: 60945 bytes (<61,000). Device ABI specification: `spec/DEVICE_ABI.json`.
